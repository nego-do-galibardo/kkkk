/* styles.css */

/* Reset básico */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Poppins', sans-serif;
}

body {
  background-color: #f9f9f9;
  color: #333;
  line-height: 1.6;
}

.container {
  width: 90%;
  max-width: 1200px;
  margin: auto;
  padding: 2rem 0;
}

/* Cabeçalho (Hero) */
.hero {
  background: linear-gradient(to right, #FFD700, #BEBEBE);
  color: #333;
  padding: 6rem 0;
  text-align: center;
}

.hero h1 {
  font-size: 3rem;
  margin-bottom: 1rem;
}

.hero p {
  font-size: 1.2rem;
  margin-bottom: 2rem;
}

.botao {
  background-color: #FFD700;
  color: #333;
  padding: 0.8rem 2rem;
  border: none;
  font-weight: bold;
  border-radius: 4px;
  text-decoration: none;
  transition: background 0.3s;
}

.botao:hover {
  background-color: #e6c200;
}

/* Navegação */
.navbar {
  background-color: #333;
  padding: 1rem 0;
}

.navbar ul {
  list-style: none;
  display: flex;
  justify-content: center;
  gap: 2rem;
}

.navbar a {
  color: #fff;
  text-decoration: none;
  font-weight: 600;
}

.navbar a:hover {
  color: #FFD700;
}

/* Seções */
.section {
  padding: 4rem 0;
}

.cinza {
  background-color: #f0f0f0;
}

h2 {
  font-size: 2rem;
  text-align: center;
  margin-bottom: 2rem;
  color: #444;
}

.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
}

.card {
  background-color: #fff;
  padding: 2rem;
  border-radius: 10px;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
  text-align: center;
}

.card h3 {
  color: #FFD700;
  margin-bottom: 1rem;
}

.card p {
  color: #555;
}

/* Estatísticas */
.estatisticas {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-around;
  gap: 2rem;
  text-align: center;
}

.estatistica {
  flex: 1;
  min-width: 200px;
}

.estatistica .numero {
  font-size: 2.5rem;
  color: #FFD700;
  font-weight: bold;
}

.estatistica p {
  margin-top: 0.5rem;
}

/* Lista de apoio */
.apoios {
  list-style: disc inside;
  max-width: 600px;
  margin: 2rem auto;
  font-size: 1.1rem;
}

/* Rodapé */
footer {
  background-color: #333;
  color: #fff;
  padding: 2rem 0;
  text-align: center;
  font-size: 0.9rem;
}

/* Responsivo */
@media (max-width: 768px) {
  .navbar ul {
    flex-direction: column;
    gap: 1rem;
  }

  .hero h1 {
    font-size: 2rem;
  }

  .hero p {
    font-size: 1rem;
  }

  .estatisticas {
    flex-direction: column;
  }
}
